# Strategist Response

## Control Fields
- allow_implementation_phase: yes
- strategist_finish_vote: no
- finish_gate_status: open
- last_strategist_decision: 先把首轮实现重点放在 `src/train/eval_reservoir.py` 与 `configs/lorenz_single.yaml` 的映射/基点/扫描策略诊断与校正；`src/data/lorenz.py` 只在证据显示训练数据分布本身异常时再改。

## Task Goal
- primary_goal: 让重新生成的 `fig_lorenz_single_critical_hist.png` 在峰位、离散度、左尾范围、真值线相对位置上与论文 `2501.01579v1` Fig.3(d) 对齐。
- non_goals: 不接受仅通过画图美化掩盖分布偏差；不优先修改与 `lorenz_single` 无关模块。

## Hard Constraints
- constraint_1: 使用 conda 环境 `pytorch`，解释器 `/home/Creeken/miniconda3/envs/pytorch/bin/python`。
- constraint_2: 必须沿 cocodex 流程推进；修改应先围绕 Lorenz 单参数数据生成与评估链路，且持续以再生图和 `reservoir/eval_summary.json` 为准验收。

## Repo Facts
- relevant_paths: `configs/lorenz_single.yaml`, `src/data/lorenz.py`, `src/train/eval_reservoir.py`, `src/models/reservoir/transition_search.py`, `README.md`, `2501.01579v1.pdf`
- confirmed_facts:
  - `configs/lorenz_single.yaml` 当前为 `use_paper_reference_mapping: false`，`base_param_strategy: target_value`，`base_param_value: 30.0`，`require_healthy_base: true`。
  - `src/train/eval_reservoir.py` 当前默认用 run-fitted `physical_from_latent` 回映射，并按映射斜率自动推断扫描方向。
  - `README.md` 明确说明这些默认值是“repo-informed completion”，并非论文硬写明。
  - `src/data/lorenz.py` 的单参数数据是 `rho in [25, 35]` 的 `solve_ivp` 轨迹，先归一化，再把论文映射只作为 `meta.paper_mapping` 保存；这里没有直接决定评估期使用 paper mapping。
  - `src/models/reservoir/transition_search.py` 的单参数搜索逻辑是“base 点健康检查 + coarse scan + binary refine”；miss 不再伪装成边界临界点。

## Recommended Architecture
- file_layout:
  - 首轮主改 `configs/lorenz_single.yaml`
  - 次改 `src/train/eval_reservoir.py`
  - 仅在证据不足以解释偏差时再动 `src/data/lorenz.py`
- ownership_boundaries:
  - `configs/lorenz_single.yaml`: 打开/关闭论文参考映射、base 策略、健康门槛的实验开关
  - `src/train/eval_reservoir.py`: 评估策略、诊断输出、回映射来源、base 选择逻辑
  - `src/data/lorenz.py`: 只负责物理轨迹生成与归一化，不承担“纠正评估分布”的职责
- implementation_notes:
  - 首轮不应盲改 ODE 或随机初值；先证明偏差主要来自映射/基点/筛选还是训练数据本身。
  - 若论文 Fig.3(d) 是物理参数空间直方图，优先验证 paper mapping 是否必须恢复为默认。

## Ordered Execution Plan
1. step: 建立当前基线并补全诊断
   - input: 当前 `configs/lorenz_single.yaml`、现有输出目录、`reservoir/eval_summary.json`
   - output: 一份基线摘要，至少包含 `mapping_used`、`base_param_value`、`status_counts`、found-only 统计、当前图与论文差异
2. step: 最小改动验证“评估策略偏差”假设
   - input: `configs/lorenz_single.yaml`, `src/train/eval_reservoir.py`
   - output: 一次或多次对照实验，优先比较 `use_paper_reference_mapping=true/false`、`base_param_strategy=max/target_value`、`require_healthy_base=true/false`
3. step: 只在必要时改数据生成
   - input: `src/data/lorenz.py` 与 latent-vs-rho 图、拟合映射质量
   - output: 若 run-fitted 映射本身异常，定位是训练样本覆盖、轨迹长度、初值策略还是归一化造成
4. step: 固化最接近论文的默认链路
   - input: 对照实验结果
   - output: 代码与配置定稿，重新生成 `fig_lorenz_single_critical_hist.png`，并更新会话状态

## Critique Of Implementer Status
- strengths: 尚未进入误改阶段，当前仍可按证据驱动首轮实现。
- weaknesses: 目前把 `src/data/lorenz.py` 设为首嫌疑目标的依据不足；仓库已暴露出更强的系统性偏差源。
- required_corrections: 首轮实现必须先否定/确认 `eval_reservoir` 的 mapping/base/health-gate 组合效应，再决定是否触碰 `lorenz.py`。

## Risks
- risk_1: 仅切回 `paper_reference_mapping` 可能让图更像论文，但若 latent->physical 拟合本身已失真，会掩盖真实链路问题。
- risk_2: `require_healthy_base` 与 `rho≈30` 的 base 选择可能显著筛掉 realization，改变 found-only 分布中心和尾部，导致对论文图的系统性偏移。

## Verification Requirements
- automated:
  - 用 `/home/Creeken/miniconda3/envs/pytorch/bin/python` 跑 Lorenz 单参数复现实验，至少重生 `fig_lorenz_single_critical_hist.png`
  - 检查 `reservoir/eval_summary.json` 的 `mapping_used`、`status_counts`、`miss_rate`、`base_*` 字段
  - 若修改 `src/data/lorenz.py`，必须同时复查 `fig_lorenz_single_latent_vs_rho.png` 和 VAE 映射摘要
- manual:
  - 与论文 Fig.3(d) 比较：主峰应主要在 `rho_c≈23-24` 附近，左尾不能长到当前异常范围，真值线 `23.99` 的相对位置应合理
  - 确认不是“found 数量骤减 + 分布看起来更尖”的假改好

## Open Questions For Implementer
- q1: 当前输出目录里，`reservoir/eval_summary.json` 的 `mapping_used`、`status_counts`、`base_param_value` 实际是什么，和异常左尾是否同向？
- q2: 在只改配置的前提下，`use_paper_reference_mapping=true` 是否已经能显著把直方图拉回论文范围；如果能，剩余误差主要是峰宽还是峰位？

## Next Step
- immediate_action: 先运行并记录一轮基线诊断，然后优先做评估链路的最小对照实验，不要先改 `src/data/lorenz.py`。
- stop_conditions:
  - 当对照结果表明评估策略已足以解释主要偏差，再允许小范围定稿修改。
  - 若改到 `src/data/lorenz.py`，必须有明确证据显示 latent-vs-rho 或物理轨迹分布本身异常；否则停止继续下沉。