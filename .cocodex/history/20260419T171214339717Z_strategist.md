# Strategist Response

## Control Fields
- last_strategist_decision: Allow eval_reservoir-only experimentation to continue. Prioritize ratio2p0_coarse50 n=30 validation. Do not rerun VAE, data generation, main reservoir training, or full reproduce. Keep base_param_value requested at 30.0, use paper_reference_mapping=true for this experiment chain, and record each config/result step in git. Do not expand to n=100 unless n=30 passes the stated gate; do not expand to n=1000 unless n=100 passes its gate.
- allow_implementation_phase: yes
- strategist_finish_vote: no
- finish_gate_status: open

## Context
- Current best small-batch candidate is ratio2p0_coarse50/60/70_n8: found=5/8, miss=0.375, mean=23.7901, median=23.8371, min=22.8012, max=25.0500, std=0.8307.
- User fixed base_param_value=30.0. Because the artifacts use a linspace parameter grid, the actual selected nearest base sample is 29.936708450317383; reports must distinguish requested base from realized base.
- The main config currently has use_paper_reference_mapping=false, while the sweep configs set it true. Implementer must record the exact temporary/resolved config used for each eval run.

## Plan
- Complete n=30 for ratio2p0_coarse50 with identical eval settings except num_realizations.
- n=30 gate to allow n=100: mapping_used.source must be paper_reference; num_realizations=30; found >= 18/30; miss_rate <= 0.40; mean and median in [23.6, 24.4]; std in [0.55, 1.20]; min >= 22.0; no long left tail below 21.
- If n=30 fails, continue eval-layer tuning at small or n=30 scale rather than expanding.
- If n=30 passes, run n=100 with identical settings except num_realizations.
- n=100 gate to allow n=1000: found >= 65/100; miss_rate <= 0.35; mean and median in [23.75, 24.25]; std in [0.60, 1.10]; min >= 22.0; visually single-peaked without long left tail.

## Assessment
- n=8 evidence is promising but too small; equal coarse50/60/70 results cannot prove coarse_steps is irrelevant.
- miss_rate=0.375 at n=8 is a risk; high miss_rate can bias found-only histogram.
- Paper reference mapping can make the histogram look correct numerically, so every result must retain mapping_used and detector settings for auditability.

## Verification
- Each run must preserve eval_summary.json, fig_lorenz_single_critical_hist.png, exact config snapshot, command/setting notes, and git commit hash.
- Confirm commands invoke only src.train.eval_reservoir.
- Confirm no VAE/data-generation/full-reproduction commands are executed.

## Open Questions
- None blocking. Continue automatically under the gates above.

## Next Step
Record n=30 results, evaluate them against the gate, commit the exact result snapshot, then either expand to n=100 if the gate passes or tune eval detector settings if it fails.
